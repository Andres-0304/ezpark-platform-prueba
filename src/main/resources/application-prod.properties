# Production Configuration for Render Deployment
spring.application.name=ezpark-platform

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/
server.compression.enabled=true

# CORS Configuration
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Production Database Configuration (PostgreSQL on Render)
# Parse DATABASE_URL format: postgresql://user:pass@host:port/dbname
spring.datasource.url=jdbc:postgresql://${DB_HOST:dpg-d15pgtmmcj7s73dlo4ag-a.oregon-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:ezpark_db_zgm2}
spring.datasource.username=${DB_USER:ezpark_db_zgm2_user}
spring.datasource.password=${DB_PASSWORD:JXQhMlzkIoXecS8p8pSda7uGXnxPqIkV}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA Configuration for Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.naming.physical-strategy=com.acme.ezpark.platform.shared.infrastructure.persistence.jpa.strategy.SnakeCasePhysicalNamingStrategy

# Handle PostgreSQL specific configurations
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Production Logging Configuration
logging.level.root=INFO
logging.level.com.acme.ezpark.platform=INFO
logging.level.org.springframework=INFO

# Temporary debugging - REMOVE after fixing
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.jdbc=DEBUG

# SQL Initialization
spring.sql.init.mode=never

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.probes.enabled=true

# Timezone Configuration for UTC handling
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
server.timezone=UTC
